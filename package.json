{
  "name": "vedanco-website-root",
  "version": "1.0.0",
  "description": "Root configuration for Vedanco Website deployment",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf dist && cp -r frontend/dist dist"
  }
}
